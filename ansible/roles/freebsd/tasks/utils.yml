---

- name: install python on {{ hostname }}
  pkgng: name=python state=present
  tags:
    - install_python
    - utils

- name: install bash-static on {{ hostname }}
  pkgng: name=bash-static state=present
  tags:
    - install_bash
    - utils

- name: install rsync on {{ hostname }}
  pkgng: name=rsync state=present
  tags:
    - install_rsync
    - utils

...
