{% for jail in jails %}
rdr pass on $ext_if  inet proto {tcp udp} to ($ext_if)  port {{ hostvars[jail].ansible_ssh_port }} -> {{ hostvars[jail].ipv4.address }}        port {{ hostvars[jail].ansible_ssh_port }}
{% endfor %}